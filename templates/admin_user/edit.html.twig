{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1 class="mx-auto my-3">Modifier l'utilisateur "{{ user.firstname }} {{ user.lastname }}"</h1>

    <div class="card m-auto p-3 border border-info bg-primary text-primary-inverse" style="width: 18rem;">
        <img src="{{ user.avatar }}" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{ user.firstname }} {{ user.lastname }}</h5>
            <h6>{{ user.email }}</h6>
            {{ include('admin_user/_form.html.twig', {'button_label': 'Modifier'}) }}

        </div>
    </div>
    <div class="d-flex flex-column align-items-center">
        <a class="btn btn-info my-3" href="{{ path('app_admin_user_index') }}">Retour à l'index</a>

        {{ include('admin_user/_delete_form.html.twig') }}
    </div>
{% endblock %}
