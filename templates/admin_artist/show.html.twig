{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>
<div class="m-5 table-responsive">
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artist.name }}</td>
            </tr>
            <tr>
                <th>DateOfBirth</th>
                <td>{{ artist.dateOfBirth ? artist.dateOfBirth|date('d/m/y') : '' }}</td>
            </tr>
            <tr>
                <th>Nationality</th>
                <td>{{ artist.nationality }}</td>
            </tr>
            <tr>
                <th>Biography</th>
                <td>{{ artist.biography }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ artist.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-2" href="{{ path('app_admin_artist_index') }}">Retour à l'index</a>

    <a class="btn btn-info mb-2" href="{{ path('app_admin_artist_edit', {'id': artist.id}) }}">Editer</a>

    {{ include('admin_artist/_delete_form.html.twig') }}
</div>
{% endblock %}
