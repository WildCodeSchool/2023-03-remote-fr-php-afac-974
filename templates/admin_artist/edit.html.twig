{% extends 'base.html.twig' %}

{% block title %}Edition de {{ artist.name }}{% endblock %}

{% block body %}
    <div class="m-5">
    <div class="d-flex justify-content-center m-4">
    <h1>Editer "{{ artist.name }}"</h1>
</div>

    {{ include('admin_artist/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="d-flex flex-row justify-content-center mb-2">
        <span class="mx-3">{{ include('admin_artist/_delete_form.html.twig') }}</span>
        <a class="btn btn-primary" href="{{ path('app_admin_artist_index') }}">Retour à l'index</a>
    </div>
{% endblock %}
